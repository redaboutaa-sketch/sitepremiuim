---
import Base from '@/layouts/Base.astro';
import Brands from '@/views/Brands.astro';

const page = 'brands' as const;
const locale = 'en' as const;
---

<Base page={page} locale={locale}>
  <Brands locale={locale} />
</Base>
