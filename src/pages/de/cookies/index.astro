---
import Base from '@/layouts/Base.astro';
import Legal from '@/views/Legal.astro';

const page = 'cookies' as const;
const locale = 'de' as const;
---

<Base page={page} locale={locale}>
  <Legal page={page} locale={locale} />
</Base>
