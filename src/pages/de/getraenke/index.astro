---
import Base from '@/layouts/Base.astro';
import Drinks from '@/views/Drinks.astro';

const page = 'drinks' as const;
const locale = 'de' as const;
---

<Base page={page} locale={locale}>
  <Drinks locale={locale} />
</Base>
