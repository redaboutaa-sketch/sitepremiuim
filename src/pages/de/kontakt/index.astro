---
import Base from '@/layouts/Base.astro';
import Placeholder from '@/views/Placeholder.astro';

const page = 'contact' as const;
const locale = 'de' as const;
---

<Base page={page} locale={locale}>
  <Placeholder page={page} locale={locale} />
</Base>
